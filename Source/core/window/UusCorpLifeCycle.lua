---@diagnostic disable: unused-local
UusCorpLifeCycle = function()
    local self = {}

    self.onInitialize = function() end

    self.onShutdown = function() end

    self.onShown = function() end

    self.onRButtonUp = function(flag, x, y) end

    self.onRButtonDown = function(flag, x, y) end

    self.onLButtonUp = function(flag, x, y) end

    self.onLButtonDown = function(flag, x, y) end

    self.onEvent = function() end

    return self
end
