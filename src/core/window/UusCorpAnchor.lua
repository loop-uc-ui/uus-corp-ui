UusCorpAnchor = {}

function UusCorpAnchor.new(anchorPoint, relativePoint, relativeTo, x, y)
    return {
        anchorPoint = anchorPoint,
        relativePoint = relativePoint,
        relativeTo = relativeTo,
        x = x,
        y = y
    }
end